% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dtsmart_launch.R
\name{dtsmart_launch}
\alias{dtsmart_launch}
\title{Launch the dtsmartr Data Explorer in your default Web Browser}
\usage{
dtsmart_launch(data = NULL, port = NULL, options = dtsmartr_options())
}
\arguments{
\item{data}{A \code{data.frame} to explore, or \code{NULL} (default) to launch the
Zero-Code Data Ingestion Wizard.}

\item{port}{Optional port number. If \code{NULL} (default), a free port is chosen automatically.}

\item{options}{Named list of UI options generated by \code{\link[=dtsmartr_options]{dtsmartr_options()}}.}
}
\description{
Launches a temporary, lightweight Shiny server in the background and opens the
interactive grid in your default external web browser. This bypasses local
browser origin security policies (CORS) that block \verb{file://} resources.
}
\details{
\subsection{Zero-Code Ingestion Wizard (Wizard Mode)}{

When \code{data = NULL} (default), the Shiny server boots into Wizard Mode. An
interactive interface (powered by the \code{datamods} package) allows users to
drag-and-drop or select:
\itemize{
\item CSV files (\code{.csv})
\item Excel files (\code{.xlsx})
\item SAS datasets (\code{.sas7bdat})
\item RDS files (\code{.rds})
}

Once a file is successfully uploaded, the wizard parses the dataset and feeds it
directly into the React-powered virtualized grid explorer.
}
}
\examples{
if (interactive()) {
  # 1. Launch wizard mode to upload local files
  dtsmart_launch()

  # 2. Launch directly with a dataset
  dtsmart_launch(mtcars)
}
}
