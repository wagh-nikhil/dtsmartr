% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save_dtsmartr.R
\name{save_dtsmartr}
\alias{save_dtsmartr}
\title{Save a dtsmartr widget to an HTML file}
\usage{
save_dtsmartr(
  data,
  file,
  selfcontained = TRUE,
  title = "dtsmartr",
  open = FALSE,
  background = "white",
  libdir = NULL,
  width = NULL,
  height = NULL,
  elementId = NULL,
  options = dtsmartr_options(),
  verbose = TRUE
)
}
\arguments{
\item{data}{A \code{data.frame} (or object coercible to one) to explore.}

\item{file}{Character string. Path to the output HTML file. The \code{.html}
extension is appended automatically if omitted.}

\item{selfcontained}{Logical. When \code{TRUE} (default), all JavaScript, CSS, and
data are embedded directly inside the HTML file, producing a single portable
file. When \code{FALSE}, a companion \verb{<file>_files/} directory is created next
to the HTML file containing the JS/CSS assets - both must be kept together
when sharing. Use \code{FALSE} for large datasets where a single file would be
impractically large.}

\item{title}{Character string. Browser tab / window title for the saved HTML
page. Defaults to \code{"dtsmartr"}.}

\item{open}{Logical. When \code{TRUE} and the session is interactive, the saved
HTML file is opened automatically in the default web browser immediately
after saving. Defaults to \code{FALSE}.}

\item{background}{Character string. CSS colour for the page background.
Defaults to \code{"white"}.}

\item{libdir}{Character string or \code{NULL}. When \code{selfcontained = FALSE}, the
path to write the dependency libraries. Passed directly to
\code{\link[htmlwidgets:saveWidget]{htmlwidgets::saveWidget()}}. Defaults to \code{NULL} (creates \verb{<file>_files/}
next to the output file).}

\item{width}{Numeric or \code{NULL}. Widget width in pixels. \code{NULL} (default)
uses the full page width.}

\item{height}{Numeric or \code{NULL}. Widget height in pixels. \code{NULL} (default)
fills the viewport (maximised mode).}

\item{elementId}{Character string or \code{NULL}. CSS id for the widget's
root \verb{<div>}. Defaults to \code{NULL} (auto-generated).}

\item{options}{Named list of UI options generated by \code{\link[=dtsmartr_options]{dtsmartr_options()}}.}

\item{verbose}{Logical. When \code{TRUE}, prints a confirmation message with the
resolved absolute path of the saved file. Defaults to \code{TRUE}.}
}
\value{
The absolute path to the saved HTML file, invisibly.
}
\description{
Exports any \code{data.frame} as a fully interactive, standalone HTML file powered
by the dtsmartr widget. The resulting file can be opened in any modern web
browser without an active R session or internet connection, making it ideal
for offline use and sharing with collaborators.
}
\details{
\subsection{Selfcontained vs. non-selfcontained}{\tabular{lll}{
   \code{selfcontained} \tab Output \tab Best for \cr
   \code{TRUE} (default) \tab Single \code{.html} file \tab Email / sharing \cr
   \code{FALSE} \tab \code{.html} + \verb{_files/} folder \tab Local use / large datasets \cr
}

}

\subsection{File size}{

Self-contained files embed the React runtime and widget code (~120 KB
gzipped) along with the data. For very large datasets (> 100 k rows) or
when generating many files, prefer \code{selfcontained = FALSE}.
}

\subsection{Browser compatibility}{

The generated HTML works in all modern browsers (Chrome, Firefox, Edge,
Safari 14+). No internet connection is required.
}
}
\examples{
\dontrun{
# == Basic usage =============================================================

# Save mtcars as a self-contained HTML (single portable file)
save_dtsmartr(mtcars, "mtcars_explorer.html")

# Open in the browser right after saving
save_dtsmartr(mtcars, "mtcars_explorer.html", open = TRUE)

# == Custom options ==========================================================

save_dtsmartr(
  mtcars, 
  "mtcars_custom.html", 
  options = dtsmartr_options(hidden_columns = "cyl", advanced_filter = FALSE)
)
}

}
\seealso{
\code{\link[=dtsmartr]{dtsmartr()}} for creating the widget object interactively,
\code{\link[htmlwidgets:saveWidget]{htmlwidgets::saveWidget()}} for the underlying save mechanism.
}
